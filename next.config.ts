import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "www.skeidar.no",
      "www.freepik.com",
      "img.freepik.com",
      "www.google.com",
      "png.pngtree.com",
    ],
  },
};

export default nextConfig;
