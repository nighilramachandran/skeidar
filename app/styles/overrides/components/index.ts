import { buttonOverride } from "./Button";
import { inputOverride } from "./Input";
import { linkOverride } from "./Link";
import { paperOverride } from "./Paper";
import { tapOverride } from "./Tab";

export const componetnsOverrides = {
  ...buttonOverride,
  ...inputOverride,
  ...linkOverride,
  ...paperOverride,
  ...tapOverride,
};
